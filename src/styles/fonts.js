'use strict';

import {
    StyleSheet
} from 'react-native';

const scalingFactors = {
    normal: 15,
    big: 7
};

const fonts = StyleSheet.create({
    normal: {
        fontSize: 24
    }
});

export {fonts, scalingFactors};
